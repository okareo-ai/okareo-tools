# Build output — .skill packages are generated, not committed
dist/*.skill
dist/*.zip
*swp
docs/

# Local tooling
node_modules/
__pycache__/
.DS_Store

# REPS workbench (reps/) — operator-local state, never committed
/results/
.env
.env.*
reps/target.json
*.pyc

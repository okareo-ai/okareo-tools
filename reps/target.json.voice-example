{
    "_comment": "REPS Workbench — Voice Target (v0). Copy to reps/target.json and point at your configured voice agent.",
    "_comment_secrets": "Never put literal secrets here. Reference an env var name, e.g. auth.api_key_env, resolved from your .env at run time.",

    "name": "my-voice-agent",
    "modality": "voice",
    "max_parallel_requests": 1,

    "voice": {
        "provider": "openai",
        "model": "gpt-realtime",
        "tts_voice": "alloy",
        "instructions": "",
        "custom_endpoint_url": ""
    },

    "auth": {
        "api_key_env": "TARGET_VOICE_API_KEY"
    }
}
